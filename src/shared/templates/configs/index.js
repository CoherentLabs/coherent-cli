const webpack = require('./webpack.template.js');
const tsconfig = require('./tsconfig.template');
const webpackReact = require('./webpackReact.template');
const babel = require('./babelrc.template');

module.exports = {
    webpack,
    tsconfig,
    webpackReact,
    babel
};
