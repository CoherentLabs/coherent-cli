const html = require('./html.template');
const config = require('./configs');
const react = require('./reactJS.template');
const store = require('./store.templates');
const model = require('./model.template');

module.exports = {
    html,
    config,
    react,
    store,
    model
};
